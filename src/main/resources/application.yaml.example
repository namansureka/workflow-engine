spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workflowdb
    username: postgres
    password: yourpassword
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  redis:
    host: localhost
    port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080